{
  "name": "keyword-scraper-render",
  "version": "1.0.0",
  "description": "Keyword research scraper on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.42.0",
    "express": "^4.18.2"
  }
}
